#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @l: The integer to find the absolute value of.
 *
 * Return: The absolute value of the integer 'l'.
 */
int _abs(int l)
{
	if (l >= 0)
	{
		return (l);
	}
	return (-l);
}
